name: Generate Share Pages

on:
  push:
    paths:
      - 'pl/new-developments/projects.json'
      - 'pl/new-developments/scripts/generate-share-pages.js'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm install sharp node-fetch

      - name: Generate share pages + images
        run: node pl/new-developments/scripts/generate-share-pages.js

      - name: Commit generated files
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add pl/new-developments/share/
          git add pl/new-developments/share-images/
          git commit -m "Auto-generate share pages and OG images" || echo "No changes"
          git push
